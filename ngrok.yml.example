version: 3

agent:
  authtoken: YOUR_NGROK_AUTH_TOKEN_HERE

endpoints:
  - name: backend_endpoint
    url: your-domain.ngrok.app  # Replace with your ngrok static domain
    upstream:
      url: http://localhost:8000

